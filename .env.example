# Database: use a connection pooler URL for Cloudflare Workers (e.g. Prisma Postgres, Neon, Supabase).
# Do not commit real values; copy to .env or .dev.vars for local dev.
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# GitHub OAuth: create an OAuth App at https://github.com/settings/developers
# Set Authorization callback URL to: https://your-domain/api/auth/callback/github (or http://localhost:3000/api/auth/callback/github for local)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Base URL for OAuth redirects (optional). Must be origin only, no path.
# e.g. http://localhost:5173 or https://your-worker.workers.dev. Defaults to VERCEL_URL or http://localhost:3000.
BETTER_AUTH_URL="http://localhost:3000"
